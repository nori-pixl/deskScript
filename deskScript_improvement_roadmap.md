<meta charset="UTF-8">

## 1. セキュリティ（サンドボックス化）の対処方針
現在の「危険な単語をリストで弾く（ブラックリスト方式）」から、**「最初から安全な空っぽの部屋に閉じ込める（ホワイトリスト方式）」**へ根本の仕組みを変更します。

### 📋 やること・設計手順
* **隔離された実行部屋（コンテキスト）の作成**
  * 式の評価（`eval` 相当の処理）を行う専用の実行空間をシステム内部に用意します。
  * Node.js環境では標準の `vm` 機能、ブラウザ環境では制限された専用環境（`iframe` やシャドウ環境）を利用します。
* **利用可能な機能のホワイトリスト化**
  * 隔離した空間に対して、開発者が明示的に許可したオブジェクト（例：`Math`、`String`、`Number` などの基本機能と、その時引き渡す変数）**だけ**を持ち込めるようにします。
  * これにより、拒否リストの隙を突いた `process` や `require` へのアクセスを物理的に遮断します。

---

## 2. パーサー（構文解析）の脆弱性の対処方針
正規表現や単純な波括弧 `{ }` のカウントによる切り貼りを卒業し、一般的なプログラミング言語と同様の**「正規の構文解析アルゴリズム」**へ移行します。

### 📋 やること・設計手順
* **トークナイザー（字句解析）の導入**
  * コードを「単なる文字列」として見るのではなく、意味のある最小単位（キーワード、変数名、文字列、記号など）の「トークン」に分解します。
  * 文字列の中にある `{` や `}` を、構文としての波括弧と誤認しないように分類します。
* **パーサー（構文木）によるネストの解決**
  * トークンの並び順から、プログラムの親子関係（ネスト構造）を表すデータ木（抽象構文木/AST）を組み立てます。
  * これにより、`NestableDispatcher` で行っている複雑な文字スキャン処理が不要になり、文字列内の `名前.add()` のようなパターンが誤爆するリスクがゼロになります。

---

## 3. メモリ管理とリソースの課題の対処方針
状態がプロセス内に溜まり続ける問題を解消し、常駐型サーバーなどでも安全に実行できるようにリソース管理を厳格化します。

### 📋 やること・設計手順
* **実行ごとの「ライフサイクル（破棄）」の導入**
  * 排他ロック、ログ、オブジェクトの共有レコードなどの状態（ステート）を管理するクラスを、スクリプトの「1回の実行」ごとに新しく生成（インスタンス化）するように変更します。
  * 実行が終了したら、その管理オブジェクトごと一括で破棄（ガベージコレクションの対象に）することで、メモリリークを完全に防ぎます。
* **回数制限から「時間制限（タイムアウト）」への移行**
  * 無限ループ対策として「1000回」といった回数のハードコーディングを廃止します。
  * 1番のサンドボックス機能と連携し、スクリプトの実行全体に「最大2秒」といった時間制限を設け、時間を超えたらシステム側から強制終了（タイムアウト例外）させる設計に変えます。

---

## 4. デバッグとエラーハンドリングの対処方針
エラーが出力文字列の中に文字として混ざってしまうのを防ぎ、**「エラーはエラーとして、結果は結果として明確に分離する」**仕組みへ変更します。

### 📋 やること・設計手順
* **例外処理（エラーのスロー）への統一**
  * パースミスやエラーを検知した際、`[Eval Warning]` という文字列を出力に足す処理を廃止します。
  * エラーを検知したその場で処理を中断し、エラー情報を上層へ通知（例外を発生）させます。
* **出力とエラーの「戻り値」の分離**
  * スクリプト実行関数が返す結果を、単なる文字列ではなく以下の要素を持ったオブジェクト（構造体）に変更します。
    1. **成否フラグ**: 実行が正常に終わったか（true / false）
    2. **出力データ**: 正常に処理された文字列（途中まで実行できた分を含む）
    3. **エラー詳細**: 失敗時のみ格納される、エラーメッセージや発生した行数などの情報
